# base/configmap.yaml (same as before)
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  FLASK_ENV: "production"
  DB_HOST: "postgres"
  DB_NAME: "appdb"
---
# base/postgres.yaml (same as before, unchanged)
# ... postgres deployment/service ...

# base/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- configmap.yaml
- postgres.yaml
